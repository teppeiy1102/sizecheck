name: sizecheck
description: "sizecheck aps"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  google_generative_ai:
  image_picker:
  flutter_dotenv:
  url_launcher:
  google_mobile_ads:


  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true
  assets:
    - .env